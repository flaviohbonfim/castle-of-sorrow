{
  "name": "castlevania-like-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "assets:build": "node tools/process-sprites.mjs",
    "assets:remove-bg": "node tools/remove-background.mjs",
    "assets:validate": "node tools/validate-assets.mjs",
    "assets:selftest": "node tools/make-test-asset.mjs && node tools/process-sprites.mjs --preview --out .assets-selftest && node tools/validate-assets.mjs --out .assets-selftest",
    "package": "npm run build && cd dist && zip -r ../castle-of-sorrow.zip . && cd .. && echo \"Wrote castle-of-sorrow.zip\""
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
